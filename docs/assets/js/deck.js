(function(){const reveal=()=>{const els=document.querySelectorAll('.reveal-on-scroll');const vh=window.innerHeight||document.documentElement.clientHeight;els.forEach(el=>{const r=el.getBoundingClientRect();if(r.top<vh-80){el.classList.add('reveal-on-scroll--visible');}});};window.addEventListener('scroll',reveal,{passive:true});window.addEventListener('load',reveal);window.addEventListener('keydown',(e)=>{const ids=Array.from(document.querySelectorAll('.slide')).map(s=>s.id);const idx=Math.max(0,ids.indexOf(location.hash.replace('#','')));if(e.key==='ArrowRight'){location.hash=ids[Math.min(idx+1,ids.length-1)]||ids[idx];}if(e.key==='ArrowLeft'){location.hash=ids[Math.max(idx-1,0)]||ids[idx];}});})();